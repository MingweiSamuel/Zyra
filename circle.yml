test:
  post:
    - mv target/*.jar $CIRCLE_ARTIFACTS
    - ls target
    - cd target/apidocs && git init && git add . && git commit -m "autogen javadoc" && git push -f https://github.com/MingweiSamuel/Zyra.git HEAD:gh-pages
